# ============================================================
# LiteAvatar - Windows CPU / Python 3.11
# ============================================================

# ------------------------------------------------------------
# PyTorch - CPU ONLY
# ------------------------------------------------------------
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.9.0+cpu
torchvision==0.24.0+cpu
torchaudio==2.9.0+cpu

# ------------------------------------------------------------
# Numerical / scientific
# ------------------------------------------------------------
numpy>=1.26.4,<2.1
scipy>=1.11,<1.15
pandas>=2.0,<3.0
sympy>=1.13,<1.15
networkx>=3.2,<4
mpmath>=1.3,<1.4

# ------------------------------------------------------------
# Image processing
# ------------------------------------------------------------
Pillow>=10.0,<12
opencv-python>=4.8,<5
imageio>=2.31,<3
imageio-ffmpeg>=0.4.9,<1

# IMPORTANT:
# Do NOT use av>=12,<15 here.
# That can cause pip to download a source tar.gz on Windows.
av==12.3.0

# ------------------------------------------------------------
# Audio
# ------------------------------------------------------------
audioread>=3.0,<4
librosa>=0.10,<1
soundfile>=0.12,<1
numba>=0.59,<0.62
llvmlite>=0.42,<0.45

# ------------------------------------------------------------
# Model / deep-learning utilities
# ------------------------------------------------------------
einops>=0.7,<1
transformers>=4.40,<5
huggingface-hub>=0.20,<1
safetensors>=0.4,<1
sentencepiece>=0.1.99
tokenizers>=0.19,<1

# ------------------------------------------------------------
# ModelScope
# ------------------------------------------------------------
modelscope>=1.8,<2

# ------------------------------------------------------------
# Configuration
# ------------------------------------------------------------
PyYAML>=6.0,<7
hydra-core>=1.3,<2
omegaconf>=2.3,<3
antlr4-python3-runtime==4.9.3

# ------------------------------------------------------------
# General utilities
# ------------------------------------------------------------
addict==2.4.0
attrs>=23,<26
certifi>=2023
charset-normalizer>=3
click>=8
colorama>=0.4
filelock>=3.13
fsspec>=2024
future>=0.18
joblib>=1.3
packaging>=23
requests>=2.31,<3
tqdm>=4.65
typing-extensions>=4.10
urllib3>=2,<3

# ------------------------------------------------------------
# Async / networking
# ------------------------------------------------------------
aiohttp>=3.9,<4
aiohappyeyeballs>=2.3
aiosignal>=1.3
frozenlist>=1.4
multidict>=6
yarl>=1.9

# ------------------------------------------------------------
# Miscellaneous packages used by LiteAvatar
# ------------------------------------------------------------
coloredlogs>=15
humanfriendly>=10
loguru>=0.7
more-itertools>=10
multiprocess>=0.70
dill>=0.3.6
editdistance>=0.8
jieba>=0.42
jaconv>=0.4
jamo>=0.4

# ------------------------------------------------------------
# Cryptography / runtime support
# ------------------------------------------------------------
cffi>=1.15
cryptography>=41,<46

# ------------------------------------------------------------
# Avatar / speech related packages
# ------------------------------------------------------------
funasr==1.0.23

# ------------------------------------------------------------
# Whisper
# ------------------------------------------------------------
openai-whisper>=20240930

# ------------------------------------------------------------
# Web/API
# ------------------------------------------------------------
fastapi>=0.100,<1
uvicorn>=0.23,<1
python-multipart>=0.0.9,<1
pydub
onnxruntime
# Pinned, not "just whatever's newest": funasr_local (bundled inside this
# project) calls the old typeguard 2.x API (check_argument_types() with no
# arguments). typeguard 3+ changed that API and also got much stricter
# about None defaults, which makes funasr_local/train/class_choices.py
# raise "TypeCheckError: argument 'default' (None) is not an instance of
# str" on import - confirmed by an actual failure with the latest typeguard.
typeguard==2.13.3
h5py

# ------------------------------------------------------------
# Sample audio generation (optional - only used by
# scripts/generate_sample_audio.py, not by the app itself)
# ------------------------------------------------------------
edge-tts>=6.1,<8